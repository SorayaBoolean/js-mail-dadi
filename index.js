const emailAddress = prompt ('Inserisci la tua email');
console.log (emailAddress)

const listaEmails = [ 'soraya@boolean.it', 'giovanni@boolean.it', 'marco@boolean.it', 'sara@boolean.it', 'michela@boolean.it', 'jacopo@boolean.it'];
console.log (listaEmails)

for (let i= 0; i< 6; i++ ) {
    console.log (i, listaEmails [i]);
}